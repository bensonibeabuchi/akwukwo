trigger:
- main

variables:
- group: akwukwo-secrets

pool:
  vmImage: ubuntu-latest

stages:
- stage: Build
  displayName: 'Build Stage'
  jobs:
    - job: Build
      displayName: 'Build'
      pool:
        vmImage: 'ubuntu-latest'
      steps:
        - checkout: self

        - task: NodeTool@0
          inputs:
            versionSpec: '18.x'
          displayName: 'Use Node.js 18'

        - script: |
            echo "📦 Installing dependencies..."
            npm ci
          displayName: 'Install dependencies'

        - task: PublishTestResults@2
          inputs:
            testResultsFormat: 'JUnit'
            testResultsFiles: '**/results.xml'
            failTaskOnFailedTests: true
          condition: succeededOrFailed()
          displayName: 'Publish test results (optional)'

- stage: Terraform
  displayName: 'Terraform Infrastructure Deployment'
  dependsOn: Build
  condition: succeeded()   
  jobs:
  - job: terraform
    displayName: 'Provision Infrastructure with Terraform'
    steps:
    # 1️⃣ Install Terraform
    - task: TerraformInstaller@1
      displayName: 'Install Terraform'
      inputs:
        terraformVersion: 'latest'

    # 2️⃣ Azure login & Terraform steps
    - task: AzureCLI@2
      displayName: 'Terraform: Init, Import, Plan, Apply'
      inputs:
        azureSubscription: 'azure-akwukwo-connection'
        scriptType: bash
        scriptLocation: inlineScript
        inlineScript: |
          echo "📦 Switching to infra directory"
          cd infra

          echo "🚀 Initializing Terraform backend..."
          terraform init -reconfigure \
            -backend-config="resource_group_name=akwukwo-rg" \
            -backend-config="storage_account_name=akwukwoterraformsa" \
            -backend-config="container_name=tfstate" \
            -backend-config="key=terraform.tfstate"

          echo "🪄 Running Terraform Plan..."
          terraform plan -out=tfplan -var-file=terraform.tfvars

          # -----------------------------
          # Import existing resources
          # -----------------------------
          if ! terraform state list | grep -q azurerm_key_vault.kv; then
            echo "📦 Importing existing Key Vault..."
            terraform import azurerm_key_vault.kv \
              /subscriptions/$(subscription_id)/resourceGroups/$(resource_group_name)/providers/Microsoft.KeyVault/vaults/$(kvName)
          else
            echo "✅ Key Vault already in Terraform state"
          fi

          if ! terraform state list | grep -q azurerm_key_vault_access_policy.app_policy; then
            echo "📦 Importing App Service Key Vault access policy..."
            terraform import azurerm_key_vault_access_policy.app_policy \
              /subscriptions/$(subscription_id)/resourceGroups/$(resource_group_name)/providers/Microsoft.KeyVault/vaults/$(kvName)/objectId/$(appPrincipalId)
          else
            echo "✅ App Service policy already in Terraform state"
          fi

          if ! terraform state list | grep -q azurerm_key_vault_access_policy.terraform; then
            echo "📦 Importing App Service Key Vault access policy TERRAFORM..."
            terraform import azurerm_key_vault_access_policy.terraform \
              /subscriptions/$(subscription_id)/resourceGroups/$(resource_group_name)/providers/Microsoft.KeyVault/vaults/$(kvName)/objectId/$(appPrincipalId)
          else
            echo "✅ App Service policy already in Terraform state"
          fi

          # -----------------------------
          # Import existing resource group
          # -----------------------------
          terraform state list | grep azurerm_resource_group.rg
          if [ $? -ne 0 ]; then
            echo "📦 Importing existing resource group into Terraform state..."
            terraform import azurerm_resource_group.rg /subscriptions/$(subscription_id)/resourceGroups/$(resource_group_name)
          else
            echo "✅ Resource group already managed by Terraform"
          fi

          # Import existing App Service if not managed
          terraform state list | grep azurerm_linux_web_app.app_with_secrets
          if [ $? -ne 0 ]; then
            echo "📦 Importing existing App Service into Terraform state..."
            terraform import azurerm_linux_web_app.app_with_secrets \
              "/subscriptions/$(subscription_id)/resourceGroups/$(resource_group_name)/providers/Microsoft.Web/sites/akwukwo"
          else
            echo "✅ App Service already managed by Terraform"
          fi

          echo "🧩 Validating Terraform configuration..."
          terraform validate

          echo "⚙️ Applying Terraform..."
          terraform plan -out=tfplan -var-file=terraform.tfvars || exit 1
          terraform apply -auto-approve tfplan
          # terraform apply -auto-approve tfplan || terraform apply -auto-approve -var-file=terraform.tfvars
      env:
        TF_VAR_dockerhub_username: $(dockerhub_username)
        TF_VAR_dockerhub_password: $(dockerhub_password)
        TF_VAR_database_url: $(database_url)
        TF_VAR_supabase_anon_key: $(supabase_anon_key)
        TF_VAR_supabase_url: $(supabase_url)
        TF_VAR_supabase_host: $(supabase_host)
        subscription_id: $(subscription_id)
        resource_group_name: $(resource_group_name)
        kvName: $(kvName)
        appPrincipalId: $(appPrincipalId)



